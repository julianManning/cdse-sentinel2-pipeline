name: sentinel2
channels:
  - defaults
dependencies:
  - python=3.11
  - jupyterlab
  - folium
  - geopandas
  - tqdm
  - gdal

# Export only the core packages you requested
conda env export --from-history > env.base.yml
